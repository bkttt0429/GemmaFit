package com.gemmafit.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary ─────────────────────────────────────────────────────────
val Green = Color(0xFF00E676)
val Red = Color(0xFFFF1744)
val Orange = Color(0xFFFF9100)
val Blue = Color(0xFF448AFF)
val PurpleHighlight = Color(0xFFE040FB)
val PurpleSecondary = Color(0xFF7C4DFF)

// ── Background ──────────────────────────────────────────────────────
val Background = Color(0xFF121212)
val Surface = Color(0xFF1E1E1E)
val OverlayPanel = Color(0xD02C2C2C) // 80% alpha

// ── Text ────────────────────────────────────────────────────────────
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFFB0B0B0)
val TextHint = Color(0xFF757575)

// ── Form Score ──────────────────────────────────────────────────────
val ScoreHigh = Color(0xFF00E676)
val ScoreMid = Color(0xFFFFAB00)
val ScoreLow = Color(0xFFFF1744)

// ── Skeleton Overlay ────────────────────────────────────────────────
val SkeletonNormal = Color(0x9900E676) // 60% alpha green
val SkeletonViolation = Color(0xE6FF1744) // 90% alpha red
val SkeletonJoint = Color(0xCC00E676) // 80% alpha green dot
val CorrectionArrow = Color(0xD9448AFF) // 85% alpha blue
val AngleArc = Color(0x80FFFFFF) // 50% alpha white
